# うごかしかた
```
$ git clone http://157.7.52.164/gitbucket/git/akatsuura/reinoare.git
$ bundle install
$ bundle exec rake db:migrate
$ bundle exec rake db:seed
$ rails s   # => http://localhost:3000

```
